import React from 'react';

const ReportViewer: React.FC = () => (
  <section style={{ marginTop: 32 }}>
    <h2>Latest Release Intelligence Report</h2>
    <p>No reports loaded. Trigger a QA workflow to generate a report.</p>
  </section>
);

export default ReportViewer;
